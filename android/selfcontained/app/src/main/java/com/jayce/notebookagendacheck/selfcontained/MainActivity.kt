package com.jayce.notebookagendacheck.selfcontained

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

/**
 * Phase 2 scaffold: final implementation will start BackendService and render
 * a WebView bound to the embedded loopback backend.
 */
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // TODO(phase2): inflate layout and bind UI state machine for backend readiness.
    }
}
